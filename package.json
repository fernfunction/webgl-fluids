{
  "name": "webgl-fluids",
  "version": "1.0.0",
  "description": "Interactive GPU-accelerated fluid simulation sandbox (WebGL2), compiled from TypeScript into a single self-contained HTML file.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10",
    "vite-plugin-singlefile": "^2.0.3"
  }
}
